{
  "devDependencies": {
    "esbuild": "^0.14.50",
    "eslint": "^8.20.0"
  },
  "dependencies": {
    "luxon": "^3.0.1"
  },
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle colorgame.js --outdir=www/js",
    "serve": "esbuild --serve=127.0.0.1:8080 --servedir=www",
    "clean": "rm -r www/js"
  }
}
